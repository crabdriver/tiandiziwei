# APK 基线来源记录（冻结信息）

本文档记录用于基线工作的 APK **文件身份与哈希**，来源说明以可核实事实为限；本轮已在 Android 模拟器中完成一次真实安装与样本提取，相关运行证据见 `runtime-setup.md`。

## 冻结日期

- **文档记录日期：** 2026-03-27（与 `runtime-setup.md` 同次环境探测）

## 文件标识

| 字段 | 值 |
|------|-----|
| 文件名 | `app.apk` |
| 绝对路径 | `/Users/wizard/work_2025/tiandiziwei/app.apk` |
| 相对主仓库根目录 | `app.apk`（与 `.gitignore` 中 `/app.apk` 对应） |
| 相对本 worktree 根目录 | `../../app.apk` |

## 与 Git 的关系

- `app.apk` 被仓库根 `.gitignore` 忽略（规则：`/app.apk`），**不再、也不应**作为仓库内可提交二进制管理；协作方需在本地自行放置同路径或等价文件，并以 **SHA256** 对齐。

## 完整性校验（SHA256）

在 **arm64** 本机上对绝对路径文件执行 `shasum -a 256` 的实测结果：

```text
c8a8aa581993061676545e4e4f31ce7c1158b08a5a6b237145ac3812fdc5e34b  /Users/wizard/work_2025/tiandiziwei/app.apk
```

## 来源说明

- **可核实事实：** 文件存在于上述绝对路径；本地大小约 95.3 MB；修改时间戳（本地 `stat`）：2026-03-14 00:11:13 +0800。  
- **分发渠道 / 构建号 / 签名方：** 本次探测**未**从仓库内其他文件自动解析到权威来源字段；若需写入「来自某渠道某版本」，须在取得可引用证据后手工更新本节。

## 基线捕获状态

- **首次真实安装：** 已在 AVD `tiandiziwei-api35`（Android 35 / Google APIs / arm64-v8a）中通过 `adb install -r` 安装成功。
- **首次真实样本：** 已记录一条来自 `ziweipan` 盘面页的真实样本。

### 样本输入（实测）

- 真太阳时参数串：`圻拆祗柝袛祇#2|2026|03|27|22|34|24|120.000000|-8|1|0`
- 北京时间参数串：`圻拆祗柝袛祇#1|2026|3|27|22|39|43|120.0|-8|1|0`

### 样本输出（已从 APK UI 读取）

- `命宫 = 辰`
- `身宫 = 寅`
- `命主 = 廉贞`
- `身主 = 铃星`

### 读取方式

- 输入串：通过 `adb root` 后读取 `/data/user/0/com.example.ziweixingyu/shared_prefs/*.xml`
- 输出字段：通过真实模拟器截图人工读取 `ziweipan` 页面

### 当前限制

- 当前可稳定复用“最近一次排盘参数串 + 盘面截图”这条链路。
- 若要批量采样不同样本，仍需补稳定的输入页导航或直接写入/驱动应用内部输入状态。
